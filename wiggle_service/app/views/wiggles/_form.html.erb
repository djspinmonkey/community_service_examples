<%= form_for(@wiggle) do |f| %>
  <% if @wiggle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wiggle.errors.count, "error") %> prohibited this wiggle from being saved:</h2>

      <ul>
      <% @wiggle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :owner_id %><br>
    <%= f.text_field :owner_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
